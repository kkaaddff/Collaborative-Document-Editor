# 前端协同 WebSocket 地址。
# Next.js 构建时会把 NEXT_PUBLIC_* 内联进静态产物，所以生产值在 build 之前必须就位。
# - 本地开发: 复制本文件为 .env.local 并保持 localhost:3001
# - 生产部署: 无需手改 —— deploy/deploy.sh 会自动写入 .env.production.local
#   (值为 deploy/config.sh 里的 WS_PUBLIC_URL)
NEXT_PUBLIC_WS_URL=http://localhost:3001
